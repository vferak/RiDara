import * as path from 'path';

export const BPMN_RESOURCE_PATH = path.join(process.cwd(), 'resources', 'bpmn');

export const BPMN_BLANK_FILE_PATH = path.join(BPMN_RESOURCE_PATH, 'blank.bpmn');

export const BPMN_FILE_EXTENSION = '.bpmn';
